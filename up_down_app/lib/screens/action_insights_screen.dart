import 'package:flutter/material.dart';

class ActionInsightsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Action Insights'),
      ),
      body: Center(
        child: Text('Action Insights Screen'),
      ),
    );
  }
}
